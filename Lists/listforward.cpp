#include <iostream>
#include <forward_list>
#define fl forward_list
using namespace std;
int main(int argc, char **argv)
{
    // create two numbers and add them to another variable
    int a = 2;
    int b = 2;
    int c = a + b;
    // output variable c to stdout
    cout << "a" << a << "b" << b << "c" << endl;
}